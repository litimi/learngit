export default {
  set_LoginInfo (state, type) {
    state.loginInfo = type
  },
  set_Token (state, type) {
    state.token = type
  },
  set_BaseInfo (state, type) {
    state.baseInfo = type
  },
  set_Username (state, type) {
    state.username = type
  },
  set_Password (state, type) {
    state.password = type
  }
}
